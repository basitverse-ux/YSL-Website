# tools/ — dev-only

Build/reference tooling. **Not deployed** — only `public_html/` and `app/` ship (CLAUDE.md §12).

- `finovate-measure.mjs` — CLAUDE.md §9 step 1 capture: screenshots + computed styles from the live
  Finovate demo into `reference/` and `docs/finovate-raw-measurements.json`. Requires web egress.
  See the header of the script for prereqs and run instructions.
