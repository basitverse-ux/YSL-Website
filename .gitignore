# Secrets — never commit (brief §6, §8, §12)
config/config.php
.env
*.env

# Finovate reference screenshots — git-ignored per brief §1, §12
reference/

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# PHP / Composer (if PHPMailer vendored later)
vendor/
composer.lock

# Node (dev-only: Playwright for §9 capture; Fontsource for font extraction)
node_modules/
package-lock.json

# Raw measurement output (regenerated by tools/finovate-measure.mjs)
docs/finovate-raw-measurements.json

# Runtime writables (rate-limit files etc.) — outside public_html in prod
var/
